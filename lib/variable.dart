void variable() {
  // does not have to be initialized
  // int age;

  int age = 0;
  double gpax = 0;
  bool status = true;
  String name = "Peter";

  print('name: $name is $age years old, gpax: $gpax');

  if (status == true) {
    print('Your status is active \n');
  }

  //shorthnad if else statement
  // print(status ? 'Your status is active' : 'Your status is inactive');
}
